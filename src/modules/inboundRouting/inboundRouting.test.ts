import { describe, it, expect } from "vitest";

import "dotenv/config";

// const { MAILERSEND_API_KEY, MAILERSEND_DOMAIN_ID } = process.env;

describe("Inbound Routing", () => {
  it.concurrent("Function", async () => {
    try {
      expect(true).toEqual(true);
    } catch (error) {
      console.error(error);
    }
  });

  it.concurrent("Add Inbound Route", async () => {
    try {
      expect(true).toEqual(true);
    } catch (error) {
      console.error(error);
    }
  });

  it.concurrent("Delete Inbound Route", async () => {
    try {
      expect(true).toEqual(true);
    } catch (error) {
      console.error(error);
    }
  });

  it.concurrent("Inbound Route by ID", async () => {
    try {
      expect(true).toEqual(true);
    } catch (error) {
      console.error(error);
    }
  });

  it.concurrent("List Inbound Routes", async () => {
    try {
      expect(true).toEqual(true);
    } catch (error) {
      console.error(error);
    }
  });

  it.concurrent("Update Inbound Route", async () => {
    try {
      expect(true).toEqual(true);
    } catch (error) {
      console.error(error);
    }
  });
});
